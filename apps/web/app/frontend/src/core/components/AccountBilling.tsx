// Core edition has no billing / credits surface — the user-dropdown
// renders nothing in this slot.

interface AccountBillingProps {
  refreshKey?: number;
}

export function AccountBilling(_props: AccountBillingProps) {
  return null;
}
