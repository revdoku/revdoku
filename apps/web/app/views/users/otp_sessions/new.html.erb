<div class="max-w-md w-full space-y-8">
  <div>
    <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900 dark:text-white">
      Sign in to your account
    </h2>
    <% if !Revdoku.google_auth_enabled? && Revdoku.email_auth_enabled? %>
      <p class="mt-2 text-center text-sm text-gray-600 dark:text-gray-400">
        We'll send a verification code to your email
      </p>
    <% end %>
  </div>

  <% if flash[:alert] %>
    <div class="bg-red-50 border border-red-200 rounded-md p-4">
      <p class="text-sm text-red-700"><%= flash[:alert] %></p>
    </div>
  <% end %>

  <% if Revdoku.google_auth_enabled? %>
    <div class="mt-6">
      <%= button_to user_google_oauth2_omniauth_authorize_path, method: :post, data: { turbo: false }, class: "w-full inline-flex justify-center items-center py-3 px-4 border border-[#747775] dark:border-gray-600 rounded-md shadow-sm bg-white dark:bg-gray-800 text-base font-medium text-[#1F1F1F] dark:text-white hover:bg-gray-50 dark:hover:bg-gray-700" do %>
        <svg class="w-5 h-5 mr-2" viewBox="0 0 24 24">
          <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
          <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
          <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
          <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
        </svg>
        Sign in with Google
      <% end %>
    </div>
  <% end %>

  <% if Revdoku.google_auth_enabled? && Revdoku.email_auth_enabled? %>
    <div class="relative">
      <div class="absolute inset-0 flex items-center">
        <div class="w-full border-t border-gray-300 dark:border-gray-700"></div>
      </div>
      <div class="relative flex justify-center text-sm">
        <span class="px-2 bg-gray-50 dark:bg-gray-950 text-gray-500 dark:text-gray-400">Or continue with email</span>
      </div>
    </div>
  <% end %>

  <% if Revdoku.email_auth_enabled? %>
    <%= form_tag(new_user_session_path, method: :post, class: "mt-8 space-y-6", data: { turbo: false }) do %>
      <div class="rounded-md shadow-sm">
        <div>
          <label for="email" class="sr-only">Email address</label>
          <input id="email" name="email" type="email" autofocus autocomplete="email" required
                 placeholder="Email address" value="<%= @email %>"
                 class="appearance-none rounded-md relative block w-full px-3 py-2 border border-gray-300 dark:border-gray-600 placeholder-gray-500 dark:placeholder-gray-400 text-gray-900 dark:text-white dark:bg-gray-800 focus:outline-none focus:ring-blue-500 focus:border-blue-500 focus:z-10 sm:text-sm" />
        </div>
      </div>

      <%= turnstile_widget %>

      <div>
        <button type="submit"
                class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 cursor-pointer">
          Send verification code
        </button>
      </div>
    <% end %>
  <% end %>

  <% if Revdoku.legal_urls_configured? %>
    <p class="mt-4 text-center text-xs text-gray-500 dark:text-gray-400">
      By signing in, you agree to our
      <%= render "shared/legal_links", style: :inline %>.
    </p>
  <% end %>

  <% if Revdoku.registration_enabled? %>
    <p class="mt-6 text-center text-sm text-gray-600 dark:text-gray-400">
      Don't have an account?
      <%= link_to "Sign up", new_user_registration_path, class: "font-medium text-blue-600 hover:text-blue-500 dark:text-blue-400 dark:hover:text-blue-300" %>
    </p>
  <% end %>
</div>
