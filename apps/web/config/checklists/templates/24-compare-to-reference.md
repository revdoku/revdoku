Compare to Reference

Compare this document against a reference the user will upload and flag every material deviation. #ref[Upload the reference] — the reference is required.

Use this template for freeform "does this match that" reviews — contract vs. template, current version vs. prior version, submitted deliverable vs. specification, response vs. RFP, and similar. The AI reads both documents and calls out every substantive difference.

- Compare the two documents section by section and enumerate every material difference. For each difference, state where in each document it appears and what changed (addition, removal, wording change, numerical change). Do not flag cosmetic differences (whitespace, font, layout) unless they change meaning. Flag any term, figure, date, name, or clause in the document under review that deviates from the reference. If the reference defines required elements, flag any required element that is missing from the document under review.
