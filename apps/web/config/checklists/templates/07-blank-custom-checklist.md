*Blank Custom Checklist

Start here to write your own checks. Each bullet below is one rule the AI will apply to the document. Edit these rules, add more, or delete them — there is no minimum or maximum. Rules are plain English imperatives; say exactly what the AI should look for and flag.

Tips:
- Keep each rule focused on ONE specific check, not "check everything about section 3".
- Give examples in the rule when the check is subjective ("flag vague entries like 'misc' or 'other'").
- To compare against a reference file the user uploads at review time, include `#ref[Upload the reference]` in the rule prompt.
- To extract a structured value for a custom script to aggregate later, include `#value — save as field,value` in the rule prompt.

Delete this entire checklist if you don't need a starting template.

- The document has a title or identifying header at the top
- All dates shown in the document are valid and not in the future
- The document does not contain placeholder text like "TBD", "[INSERT]", "XXX", or lorem ipsum
- Every page of the document is legible (no blurred pages, no pages cropped mid-line, no rotated pages)
