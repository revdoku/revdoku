# AI Project Workspace

Private working files for collaborating with AI agents.

Start with `project-brief.md`, keep agent-facing context in `agent-handoff.md`, and add source files alongside them. Nothing in this bucket is public unless its owner explicitly publishes it.
