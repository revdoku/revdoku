# Screenshot Gallery

This bucket contains example screenshot image files from the Revdoku website
asset set.

The files under `screenshots/` are screenshots, not hand-built web pages. This
template intentionally does not include `index.html`; when published, Revdoku
will generate an Auto-Index Page with previews for the images.
