# Press Kit

This bucket is a starter file collection, not a hand-built landing page.

It intentionally does not include `index.html`. When published, Revdoku will
serve the generated Auto-Index Page so visitors can browse and preview the
documents and media in this bucket.
