Connect this AI to Revdoku so it can create, update, and publish websites for me. Read and follow https://revdoku.com/prompt.md. First check whether Revdoku tools are already available; if they are, call revdoku_status and bucket_list. If they are not, do not claim you can install an app from this chat. Tell me the exact connection step supported by this AI client and wait for me to complete it. Use https://app.revdoku.com/mcp for a compatible remote MCP client. Revdoku OAuth lets me sign in or create an account in the same browser flow and returns a one-time code to the initiating client; never put a bearer token or verification code in a signup link. After approval, ask me to return to the original chat and send "continue". If Revdoku tools are not visible there, tell me to enable or select Revdoku for that conversation or start a new chat. Do not send me through signup again, and reconnect only if the client reports expired or invalid authorization. Never ask me for a password, API key, protected-site password, or verification code. If this client cannot use Revdoku's write-capable tools, say so and offer the Revdoku dashboard signup fallback; do not imply that fallback connects the AI. Once connected, if my account has no buckets, offer the suggested first projects, including an app idea landing page with a waitlist. Create my choice as a private draft and show a temporary preview after I choose it. Publish it live only after I explicitly approve publishing. If I selected a project later in this prompt, follow that choice instead of asking again.
